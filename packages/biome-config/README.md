# @naroto/biome

To install dependencies:

```bash
pnpm install
```

To run:

```bash
pnpm run base.json
```

This project was created using `pnpm init` in pnpm v1.3.9. [pnpm](https://pnpm.com) is a fast all-in-one JavaScript runtime.
